name: Build/Destroy Infrastructure AWS

on:
  workflow_call:
    inputs:
      action:
        description: 'Choose action'
        required: true
        default: 'build'
        type: string
      branch_name:
        description: 'Branch name'
        required: true
        type: string
      aws_region:
        description: 'AWS region'
        required: false
        default: 'us-east-1'
        type: string

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  AWS_ACCOUNT_ID: ${{ secrets.AWS_ACCOUNT_ID }}

jobs:
  infra-build:
    name: Build Infra
    runs-on: ${{ vars.RUNNER }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      
      - name: Run Terraform Init
        working-directory: terraform
        run: |
          terraform init \
            -backend-config="region=${{ inputs.aws_region }}" \
            -backend-config="bucket=terraformstate001-${{ inputs.branch_name }}" \
            -backend-config="key=${{ inputs.branch_name }}/terraform.tfstate" \
            -backend-config="dynamodb_table=terraform-locks-${{ inputs.branch_name }}"

      - name: Select or Create Workspace
        working-directory: terraform
        run: |
          terraform workspace select ${{ inputs.branch_name }} || terraform workspace new ${{ inputs.branch_name }}

      - name: Run Plan or Destroy
        working-directory: terraform
        id: plan_or_destroy
        run: |
          if [ "${{ github.event_name }}" == "push" ]; then
            terraform plan -var-file="environments/${{ inputs.branch_name }}/terraform.tfvars" -out=${{ inputs.branch_name }}-plan.out
          elif [ "${{ inputs.action }}" == "build" ]; then
            terraform plan -var-file="environments/${{ inputs.branch_name }}/terraform.tfvars" -out=${{ inputs.branch_name }}-plan.out
          elif [ "${{ inputs.action }}" == "destroy" ]; then
            terraform destroy -var-file="environments/${{ inputs.branch_name }}/terraform.tfvars" -auto-approve
          fi

      - name: Run Apply (if building)
        if: ${{ github.event_name == 'push' || inputs.action == 'build' }}
        working-directory: terraform
        run: |
          terraform apply ${{ inputs.branch_name }}-plan.out